services:
  asset-api:
    build: .
    container_name: asset_detection-api
    ports:
      - "8003:8000"
    restart: always
   #deploy:
    #  resources:
     #   limits:
      #    cpus: "2.0"
       #   memory: 4G